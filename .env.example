# Disposable local Compose fixtures only. Never use these values outside local development.
ENVIRONMENT=local
OPERATOR_A_TOKEN=local-operator-a-token
OPERATOR_B_TOKEN=local-operator-b-token
ADMIN_TOKEN=local-admin-token
